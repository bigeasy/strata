sudo: false

language: node_js

node_js:
  - '12'

branches:
  only:
    - master
    - sketch
    - travis-ci

install:
  - npm install --no-package-lock --no-save
  - npm install -g nyc mocha

script:
  - nyc npm test
  - (cd es6 && nyc npm test)
