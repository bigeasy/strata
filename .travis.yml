sudo: false

branches:
  only:
    - master
    - travis-ci

node_js:
   - '12'
   - '10'

env:
- SUBDIR=es6 MOCHA=mocha
- SUBDIR=efcb

matrix:
  exclude:
  - node_js: '10'
    env: SUBDIR=es6

before_install:
- cd $SUBDIR
install:
- npm install --no-package-lock --no-save
- npm install -g nyc $MOCHA
script:
- nyc npm test
